/** Real-time shadow maps (directional light + castShadow meshes). Off by default — StoneGround does not receive shadows, so the pass is invisible GPU work. */
export const ENABLE_REALTIME_SHADOWS = false;
