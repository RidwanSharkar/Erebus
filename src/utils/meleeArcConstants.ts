/** Sword / Runeblade LMB arc — preview (`performMeleeDamage`) + mesh `performSwingDamage` + mushrooms. */

export const MELEE_ARC_RANGE = 4;

/** `dot(flatToEnemy, flatForward) > this` (~120° full aperture vs forward on XZ). */
export const MELEE_ARC_MIN_DOT = -0.5;
